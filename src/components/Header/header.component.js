import React from 'react';
import { HeaderStyle } from './header.style';

const Header = () => (
  <header>
    <HeaderStyle>
      <div>
        <h1>Meal Plan!</h1>
      </div>
    </HeaderStyle>
  </header>
);

export default Header;
